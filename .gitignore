.venv/
__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
dist/
build/


# Code generated
memory/
output/

# Local infra (lives at the main checkout, not the repo, so it survives
# worktree cleanup; holds private key material — must never be tracked)
/certs/

# Test files
tests/fixtures/*/annotated/


# VQA-suggested domain fix (consult_vqa_for_pddl_domain); overrides
# robot_domain.pddl for plan_pddl when present — delete to reset
pddl/robot_domain_fixed.pddl
